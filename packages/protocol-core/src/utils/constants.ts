/** Maximum items per subgraph page query. Used by leaderboard-server and analytics. */
export const SUBGRAPH_PAGE_SIZE = 1000

/** Minimum USD price threshold. Tokens below this are excluded from analytics. */
export const MIN_PRICE_USD = 0.000001

/** Minimum USD volume threshold. Pairs below this are excluded from analytics. */
export const MIN_VOLUME_USD = 0.0001
